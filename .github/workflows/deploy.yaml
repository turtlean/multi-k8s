name: Deploy MultiDocker
on:
  push:
    branches:
      - master # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t lmatayoshi/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true lmatayoshi/react-test npm test

      - run: docker build -t lmatayoshi/multi-client ./client
      - run: docker build -t lmatayoshi/multi-nginx ./nginx
      - run: docker build -t lmatayoshi/multi-server ./server
      - run: docker build -t lmatayoshi/multi-worker ./worker

      - run: docker push lmatayoshi/multi-client
      - run: docker push lmatayoshi/multi-nginx
      - run: docker push lmatayoshi/multi-server
      - run: docker push lmatayoshi/multi-worker
